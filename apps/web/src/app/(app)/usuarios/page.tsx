'use client'; import { useData } from '../../../lib/use-data'; type U={id:string;name:string;email:string;role:string;active:boolean}; export default function Usuarios(){const {data}=useData<U[]>('/users');return <><div className="page-head"><div><h1>Usuários</h1><p>Papéis do tenant atual.</p></div></div><div className="table-wrap"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Papel</th><th>Status</th></tr></thead><tbody>{data?.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td><span className="badge">{u.role}</span></td><td>{u.active?'ATIVO':'INATIVO'}</td></tr>)}</tbody></table></div></>}
