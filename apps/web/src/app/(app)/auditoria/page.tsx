'use client'; import type { AuditEvent } from '../../../lib/types'; import { useData } from '../../../lib/use-data'; export default function Auditoria(){const {data}=useData<AuditEvent[]>('/audit');return <><div className="page-head"><div><h1>Auditoria</h1><p>Trilha de ações do tenant.</p></div></div><div className="timeline">{data?.length?data.map(a=><div className="timeline-item" key={a.id}><div className="timeline-time">{new Date(a.at).toLocaleString('pt-BR')}</div><div><strong>{a.userName}</strong> · {a.action}<div className="small muted">{a.entityType} {a.entityId} — {a.details}</div></div></div>):<div className="empty card">Nenhum evento.</div>}</div></>}
