'use client'; import { useData } from '../../../lib/use-data'; import type { AuditEvent } from '../../../lib/types';
const money=(v:number)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v||0);
export default function Dashboard(){const {data}=useData<{customers:number;pipeline:number;contracts:number;invoices:number;audit:AuditEvent[]}>('/dashboard');if(!data)return <div className="empty">Carregando...</div>;const metric=(l:string,v:string|number,d:string)=><div className="card"><div className="kpi-label">{l}</div><div className="metric">{v}</div><div className="small muted">{d}</div></div>;return <><div className="page-head"><div><h1>Dashboard</h1><p>Visão executiva do tenant atual.</p></div></div><div className="grid cols-4">{metric('Clientes',data.customers,'cadastros ativos')}{metric('Pipeline',money(data.pipeline),'valor em propostas')}{metric('Contratos',data.contracts,'gerados')}{metric('Cobranças',data.invoices,'emitidas')}</div><div className="grid cols-2" style={{marginTop:16}}><div className="card"><h3>Fluxo principal</h3><div className="list">{['Cliente cadastrado','Proposta criada e enviada','Proposta aceita','Contrato gerado','Tarefas atribuídas','Cobrança emitida e pagamento simulado'].map((x,i)=><div className="list-item" key={x}>{i+1}. {x}</div>)}</div></div><div className="card"><h3>Últimas ações</h3><div className="timeline">{data.audit.length?data.audit.map(a=><div className="timeline-item" key={a.id}><div className="timeline-time">{new Date(a.at).toLocaleString('pt-BR')}</div><div><strong>{a.userName}</strong> · {a.action}<div className="small muted">{a.entityType} {a.entityId} — {a.details}</div></div></div>):<div className="empty">Nenhuma ação ainda.</div>}</div></div></div></>}
