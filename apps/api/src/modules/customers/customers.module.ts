import { Module } from '@nestjs/common'; import { PrismaService } from '../../common/prisma.service'; import { SessionGuard } from '../../common/auth'; import { CustomersController } from './customers.controller'; import { AuditWriter } from '../../common/audit.service'; @Module({controllers:[CustomersController],providers:[PrismaService,AuditWriter,SessionGuard]}) export class CustomersModule{}