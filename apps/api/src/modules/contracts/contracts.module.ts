import { Module } from '@nestjs/common'; import { PrismaService } from '../../common/prisma.service'; import { SessionGuard } from '../../common/auth'; import { AuditWriter } from '../../common/audit.service'; import { ContractsController } from './contracts.controller'; @Module({controllers:[ContractsController],providers:[PrismaService,AuditWriter,SessionGuard]}) export class ContractsModule{}