import { Controller, Get, UseGuards } from '@nestjs/common'; import { CurrentSession, requirePermission, Session, SessionGuard } from '../../common/auth'; import { PrismaService } from '../../common/prisma.service'; @UseGuards(SessionGuard) @Controller('audit') export class AuditController{constructor(private db:PrismaService){} @Get() list(@CurrentSession() s:Session){requirePermission(s,'audit'); return this.db.auditEvent.findMany({where:{tenantId:s.tenantId},orderBy:{at:'desc'}})}}
